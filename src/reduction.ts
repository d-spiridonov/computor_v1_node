// interface IParts {
//     first: 
// }

// const convertToTerms(exp: string, parts)

const reduce = (expression: string) => { // simplify the equation and return parts
    let parts = {
        first: [],
        second: []
    }
    // parts = convertToTerms(expression, parts)
}